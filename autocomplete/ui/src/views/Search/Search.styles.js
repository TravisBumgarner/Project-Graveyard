import styled from 'styled-components';

import {
  DefaultCard
} from "../../theme";

const HomeCard = DefaultCard.extend`
`;

const Input = styled.input`
  flex: 1;
`

export {
  HomeCard,
  Input,
}

